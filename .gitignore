node_modules/
.next/
coverage/
.DS_Store
*.log
.env*
!.env.example
out/
venv/

# Build output — generated by npm run dist:exe / electron:build / build:backend
# (android/ has its own nested .gitignore for its build artifacts)
dist-electron/
backend/build/
backend/dist/
output/

# Sensitive data — never commit
backend/users.json
backend/*.csv
backend/*.xls
backend/*.xlsx

# Python cache
backend/__pycache__/
**/__pycache__/
*.pyc

# Claude local settings
.claude/settings.local.json

graphify-out/